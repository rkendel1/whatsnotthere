[package]
name = "xray-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[package.metadata.wasm-pack.profile.release]
wasm-opt = false

[dependencies]
serde_json = "1"
wasm-bindgen = "0.2"
xray-core = { path = "../core" }
